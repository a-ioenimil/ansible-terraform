[defaults]
inventory = inventory.yml
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
bin_ansible_callbacks = True

[inventory]
# Enable inventory plugins
enable_plugins = yaml, aws_ec2, ini, host_list
